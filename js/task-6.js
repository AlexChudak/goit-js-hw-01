let input;
let total = 0;

input = prompt('Введите число, оно будет добавлено к следующему');

total += input;
if (input >= 1) {
} else input === null;
{
  alert(`Общая сумма чисел равна ${total}`);
}
